version: 1
frontend:
  phases:
    build:
      commands:
        - npm install
        - npm run build
        - npx cypress run
  artifacts:
    baseDirectory: 
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*